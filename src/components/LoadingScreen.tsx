/**
 * Re-export van common/LoadingScreen zodat overal dezelfde loading-UI en tekst ("Even bezig ...") wordt getoond.
 * Voorkomt wisseling tussen "App is aan het laden" / "De app wordt opgestart" en "Even bezig".
 */
export { default } from '@/components/common/LoadingScreen';
